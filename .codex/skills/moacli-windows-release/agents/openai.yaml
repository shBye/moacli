interface:
  display_name: "MoaCLI Windows Release"
  short_description: "Build, push, and publish a MoaCLI Windows release"
  default_prompt: "Use $moacli-windows-release to build, verify, and publish the next MoaCLI Windows release."
