# MoaCLI Roadmap

이 문서는 1차 프로토타입 이후 우선 개발할 기능의 제품 흐름과 구현 기준을 정의합니다. 현재 CLI가 제공하는 Git worktree 기능은 MoaCLI가 중복 구현하지 않습니다.

## 우선순위

1. 통합 알림 센터
2. 재시작 후 세션 복구
3. 전체 대화 검색

세 기능은 서로 독립적으로 배포할 수 있어야 합니다. 다만 세션 상태, 알림, 검색 인덱스를 안정적으로 보존하기 위해 공통 SQLite 저장소와 스키마 마이그레이션 기반을 먼저 마련합니다. OAuth 토큰, API 키, 비밀번호는 데이터베이스에 저장하지 않습니다.

## 1. 통합 알림 센터

### 목표

여러 Claude, Codex, Gemini 세션을 동시에 실행할 때 현재 보고 있지 않은 세션의 입력 요청, 완료, 실패를 놓치지 않게 합니다. 알림은 CLI를 대신 제어하는 기능이 아니라 사용자를 정확한 터미널로 이동시키는 기능입니다.

### 사용자 흐름

1. 백그라운드 세션에서 주목할 이벤트가 발생합니다.
2. 왼쪽 세션 항목과 상단 알림 아이콘에 읽지 않은 개수가 표시됩니다.
3. MoaCLI가 비활성 상태면 Windows 알림도 표시합니다.
4. 알림을 누르면 앱이 활성화되고 해당 세션의 CLI 탭으로 이동합니다.
5. 세션을 확인하면 관련 알림을 읽음 처리합니다.

### 이벤트 종류

| 이벤트 | 의미 | Windows 알림 기본값 |
| --- | --- | --- |
| `needs_attention` | 승인 또는 사용자 입력 가능성이 높음 | 켜짐 |
| `completed` | 실행 프로세스가 정상 종료됨 | 켜짐 |
| `failed` | 시작 실패 또는 비정상 종료 | 켜짐 |
| `account_changed` | 로그인 후 인증 계정이 변경됨 | 켜짐 |
| `info` | 버전 변경 등 일반 상태 변화 | 꺼짐 |

`needs_attention`은 모든 CLI가 구조화 이벤트를 제공하지 않으므로 단계적으로 감지합니다.

1. 확정 신호: PTY 종료 코드, 시작 실패, 터미널 BEL, MoaCLI가 직접 수행한 로그인 결과
2. 에이전트 어댑터 신호: ANSI 코드를 제거한 최근 출력에서 알려진 승인·입력 프롬프트를 감지
3. 약한 추정 신호: 출력 후 일정 시간 정지 같은 휴리스틱은 알림 제목을 `입력 확인 필요`로 제한하고 승인 요청으로 단정하지 않음

CLI별 문자열 규칙은 공통 UI 코드에 넣지 않고 `agentId`별 감지 어댑터로 분리합니다. 버전 업데이트로 규칙이 달라질 수 있으므로 어댑터 오류가 PTY 입출력에 영향을 주면 안 됩니다.

### UI

- 타이틀바에 벨 아이콘과 읽지 않은 개수 표시
- 알림 패널에 에이전트 아이콘, 계정, 세션 제목, 이벤트 종류, 발생 시각 표시
- 세션 목록에는 가장 높은 우선순위 이벤트 하나만 배지로 표시
- 전체 읽음 처리, 항목별 삭제, 세션별 음소거 지원
- 집중 모드에서는 Windows 알림만 끄고 앱 내부 배지는 유지
- 알림 본문에는 기본적으로 원문 프롬프트나 터미널 출력 내용을 포함하지 않음

### 상태와 저장

```text
notifications
  id
  session_id
  agent_id
  account_id
  type
  title
  created_at
  read_at
  dedupe_key
```

- Main 프로세스가 세션 이벤트의 단일 작성자가 됩니다.
- 동일 세션의 이벤트는 발생 순서를 보존합니다.
- 같은 `dedupe_key`가 짧은 시간 안에 반복되면 한 항목으로 합칩니다.
- 읽은 알림은 기본 7일, 읽지 않은 알림은 기본 30일 보존합니다.
- 세션 삭제와 알림 삭제는 분리합니다. 세션이 사라져도 알림 기록은 보존 기간까지 남을 수 있습니다.

### 단계별 구현

**MVP**

- 정상 종료, 비정상 종료, 시작 실패 알림
- 앱 내부 읽지 않음 배지
- 백그라운드 Windows 알림과 클릭 이동
- 세션별 음소거

**확장**

- BEL 및 CLI별 입력 요청 감지
- 알림 필터와 보존 기간 설정
- 반복 작업 완료음을 포함한 사용자 지정 알림 규칙

### 완료 조건

- 동시에 10개 세션이 출력해도 알림 순서가 뒤섞이거나 중복 폭증하지 않습니다.
- 현재 보고 있는 세션의 일반 이벤트는 Windows 알림을 만들지 않습니다.
- Windows 알림을 누르면 올바른 계정과 세션의 CLI 탭으로 이동합니다.
- 알림 기능을 꺼도 PTY 데이터 경로와 세션 실행에는 영향이 없습니다.

## 2. 재시작 후 세션 복구

### 목표와 경계

앱 재시작이나 비정상 종료 후 사용자가 작업하던 세션 탭, 선택 상태, 계정, 작업 경로를 복원합니다.

Electron과 함께 종료된 PTY 프로세스에 그대로 재접속하는 기능은 MVP 범위가 아닙니다. 복구된 탭은 `복구 필요` 상태로 표시하고, CLI 히스토리에서 확인된 `resumeId`가 있으면 공식 resume 명령으로 새 PTY를 시작합니다.

### 저장 대상

```text
runtime_sessions
  id
  agent_id
  account_id
  cwd
  title
  purpose
  resume_id
  history_key
  folder_id
  selected_view
  last_viewed_at
  last_activity_at
  restore_state
```

- 최대 10개 세션 설명자
- 마지막 활성 세션 ID
- CLI 또는 대화 전문 중 마지막으로 선택한 탭
- 논리 폴더 배치와 세션 표시 순서
- 앱이 정상 종료되었는지 나타내는 종료 마커

터미널 스크롤백 전체와 로그인 세션은 복구하지 않습니다. 자격증명도 복제하거나 저장하지 않습니다.

### `resumeId` 연결

새 CLI 세션은 시작 순간에 `resumeId`를 알 수 없을 수 있습니다. 다음 기준으로 히스토리 파일과 실행 세션을 연결합니다.

1. 세션 시작 시각, 에이전트, 계정 설정 폴더, cwd를 기록합니다.
2. 히스토리 감시자가 같은 계정에서 새 기록을 발견합니다.
3. 생성 시각과 cwd가 실행 세션에 대응하면 `historyKey`와 `resumeId`를 연결합니다.
4. 후보가 여러 개면 자동 연결하지 않고 Recent 항목에서 사용자가 선택하게 합니다.

### 시작 시 복구 흐름

1. 저장된 세션 설명자를 읽고 마지막 레이아웃을 먼저 그립니다.
2. 설치된 CLI, 계정, 설정 폴더, cwd 존재 여부를 다시 확인합니다.
3. 복구 가능한 탭에는 `Resume`, 정보가 부족한 탭에는 `새 세션으로 시작`을 표시합니다.
4. 사용자가 실행을 선택할 때만 PTY를 생성합니다.
5. 한 번에 여러 탭을 복구할 경우 최대 동시 실행 수 10개를 그대로 적용합니다.

기본값은 수동 복구입니다. 앱 시작과 동시에 여러 CLI가 실행되거나 승인 요청을 발생시키지 않도록 자동 resume은 별도 선택 옵션으로만 제공합니다.

### 예외 처리

- 계정 없음: 필요한 계정과 설정 폴더를 표시하고 실행을 막음
- CLI 없음: 감지된 설치 상태와 재확인 버튼 제공
- cwd 없음: 새 작업 폴더를 선택한 뒤 실행 가능
- resume 기록 삭제됨: 새 세션 시작으로 전환
- 비정상 종료: 모든 이전 `running` 상태를 `복구 필요`로 정규화
- 앱 업데이트로 스키마 변경: 트랜잭션 마이그레이션 실패 시 원본 DB를 보존하고 읽기 전용 복구 안내

### 저장 방식

- React 상태가 바뀔 때마다 파일을 직접 덮어쓰지 않고 Main 프로세스의 직렬 쓰기 큐를 사용합니다.
- 세션 추가, 닫기, 활성 탭 변경, resume ID 연결은 작은 트랜잭션으로 기록합니다.
- 정상 종료 시 종료 마커를 기록하되, 복구 정확성이 종료 이벤트 하나에만 의존하지 않게 합니다.

### 완료 조건

- 앱을 강제 종료한 뒤 다시 실행해도 이전 탭 순서와 활성 탭이 복원됩니다.
- 로그인 탭과 자격증명은 복구 목록에 남지 않습니다.
- 유효한 `resumeId`를 가진 Claude, Codex 등 지원 에이전트는 동일 계정과 cwd로 재개됩니다.
- 누락된 계정이나 경로 때문에 앱 전체 복구가 실패하지 않고 해당 탭만 오류 상태가 됩니다.
- 복구된 탭은 사용자가 실행하기 전까지 메모리 사용이 작은 정적 뷰로 유지됩니다.

## 3. 전체 대화 검색

### 목표

Claude, Codex, Gemini, OpenCode의 로컬 대화를 하나의 검색 화면에서 찾습니다. 원본 히스토리 파일은 각 CLI가 소유하며 MoaCLI는 읽기 전용 인덱스만 유지합니다.

### 사용자 흐름

1. 사이드바 검색 또는 `Ctrl+K` 명령 팔레트에서 검색어를 입력합니다.
2. 결과는 메시지 문맥, 세션 제목, 에이전트, 계정, cwd, 날짜를 함께 보여줍니다.
3. 결과를 누르면 대화 전문을 열고 일치한 메시지 위치로 이동합니다.
4. 별도 `CLI에서 재개` 명령을 선택했을 때만 resume 세션을 실행합니다.

### 필터

- 에이전트: Claude, Codex, Gemini, OpenCode
- 계정 이메일 또는 계정 ID
- 논리 폴더
- 실제 작업 경로
- 사용자 메시지 또는 어시스턴트 메시지
- 날짜 범위
- 현재 존재하는 대화 또는 원본이 삭제된 대화

고급 검색은 `agent:codex`, `account:email`, `path:project`, `before:YYYY-MM-DD`, `after:YYYY-MM-DD` 형태를 지원할 수 있지만, 일반 검색창 사용을 우선합니다.

### SQLite 인덱스

```text
conversation_sources
  id, agent_id, account_id, source_path, mtime, size, deleted_at

conversations
  id, source_id, resume_id, title, cwd, started_at, updated_at

messages
  id, conversation_id, ordinal, role, content, created_at

messages_fts
  content, title, cwd
```

- `messages_fts`는 SQLite FTS5 가상 테이블로 구성합니다.
- 원본 파일 경로, 수정 시각, 크기와 간단한 지문으로 변경 여부를 판단합니다.
- 안정 키는 에이전트, 계정, 원본 세션 ID를 조합해 계정 간 같은 ID가 충돌하지 않게 합니다.
- 검색 DB는 `%APPDATA%/MoaCLI` 계열 사용자 데이터 디렉터리에 저장합니다.

Electron 버전과 네이티브 모듈 ABI를 고려해 SQLite 드라이버를 선택합니다. 네이티브 드라이버를 사용할 경우 현재 `node-pty`와 마찬가지로 패키징 및 Electron ABI 재빌드를 CI에서 검증해야 합니다.

### 증분 인덱싱

1. 앱 시작 시 기존 메타데이터를 먼저 표시해 초기 화면을 막지 않습니다.
2. 계정별 히스토리 감시자가 변경된 파일만 인덱싱 큐에 넣습니다.
3. 파일 파싱은 에이전트별 기존 히스토리 어댑터를 재사용하되 UI 렌더링과 분리합니다.
4. 큰 파일은 배치로 읽고 트랜잭션 단위로 반영합니다.
5. 같은 파일의 연속 변경은 debounce하고 계정별 작업은 직렬화합니다.
6. 인덱싱 중에도 기존 인덱스 검색은 가능하게 합니다.

### CLI에서 원본 대화를 삭제한 경우

- 파일 감시와 주기적 전체 조정에서 원본 삭제를 확인합니다.
- 해당 source를 `deleted_at`으로 먼저 표시하고 검색 기본 결과에서 제외합니다.
- 연결된 논리 폴더 정보는 짧은 유예 기간 동안 유지해 일시적인 파일 이동과 삭제를 구분합니다.
- 유예 기간 이후 메시지와 FTS 행을 트랜잭션으로 제거합니다.
- 사용자가 즉시 동기화를 실행할 수 있는 `대화 다시 검색` 명령을 제공합니다.

### 검색 UI 상태

- 첫 인덱싱: 전체 진행률 대신 `발견한 파일 / 처리한 파일`을 표시
- 증분 인덱싱: 검색창 옆 작은 activity 표시만 사용
- 파싱 실패: 검색 전체를 막지 않고 실패한 계정과 파일 수를 진단 화면에 표시
- 결과 없음: 적용된 필터를 보여주고 필터 초기화 제공
- 오래된 인덱스: 마지막 동기화 시각과 다시 빌드 명령 제공

### 개인정보와 보안

- 검색 인덱스는 로컬에만 저장합니다.
- 자격증명 파일과 토큰은 인덱싱 대상에서 명시적으로 제외합니다.
- Windows 알림에는 검색된 대화 원문을 노출하지 않습니다.
- 계정 삭제 시 해당 계정 인덱스 삭제 여부를 확인하고, 기본값은 함께 삭제입니다.
- `검색 인덱스 전체 삭제 및 재생성` 기능을 제공합니다.

### 성능 목표

- 기존 인덱스가 있을 때 앱 시작 화면을 인덱싱 완료까지 차단하지 않음
- 일반 검색 결과 첫 페이지를 150ms 이내에 표시하는 것을 목표로 함
- 한 계정의 파일 변경이 다른 계정의 검색을 막지 않음
- 대용량 히스토리 파싱 중 renderer 메인 스레드가 멈추지 않음
- 인덱스 손상 시 원본 CLI 히스토리에 손대지 않고 재생성 가능

### 단계별 구현

**MVP**

- SQLite 스키마와 마이그레이션
- Claude, Codex 메시지 FTS 검색
- 에이전트, 계정, cwd 필터
- 결과에서 대화 전문의 정확한 메시지로 이동
- 수동 전체 재인덱싱

**확장**

- Gemini, OpenCode 인덱싱
- 논리 폴더와 날짜·역할 필터
- 파일 감시 기반 실시간 증분 반영
- 고급 검색 문법과 저장된 검색 조건

### 완료 조건

- 서로 다른 서비스에서 같은 이메일이나 같은 세션 ID가 있어도 결과가 섞이지 않습니다.
- 검색 결과에서 대화를 열었을 때 일치한 메시지가 강조되고 화면 안에 위치합니다.
- CLI에서 대화를 삭제하면 다음 동기화 후 기본 결과에서 사라집니다.
- 인덱스를 삭제해도 원본 대화, 계정 설정, 논리 폴더에는 영향이 없습니다.

## 공통 구현 순서

1. SQLite 연결, 스키마 버전, 트랜잭션 마이그레이션
2. Main 프로세스 세션 이벤트 버스와 알림 MVP
3. 세션 설명자 저장과 시작 시 정적 탭 복원
4. 실행 세션과 새 히스토리의 `resumeId` 연결
5. Claude와 Codex 대화 FTS 인덱스
6. 검색 결과 이동과 삭제 동기화
7. 입력 대기 감지 어댑터와 나머지 에이전트 검색 확장

각 단계는 기존 PTY 실행과 대화 목록 기능을 유지한 상태로 빌드와 수동 회귀 검증을 통과해야 합니다.
